# 12 复习布尔变量和子集提取

2026/3/25

写在前面：暂无 : ) 

---

* 讲了一个新的内置函数`rep`（`repeat`，将某个元素重复若干次）：
    ```R
    rep("A", 30) # 将“A”重复30次
    ```
* `NA`在条件判断里会产生错误结果！（条件满不满足，都会把`NA`囊括进来，所以必须手动去除！）
* 剩下的内容请参考`R101/12 复习布尔变量和子集提取.R`文件。